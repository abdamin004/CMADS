# Thesis source (LaTeX) is kept locally only — the GitHub repo publishes
# the compiled PDF and nothing else. The allowlist below is the only path
# that travels with the repo from this directory.
*
!.gitignore
!main.pdf
