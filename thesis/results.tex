\chapter{Results}
\label{chap:results}

% ─────────────────────────────────────────────────────────────────────────────
\section{Overview}
\label{sec:results_overview}
% ─────────────────────────────────────────────────────────────────────────────

This chapter reports the results of the multi-agent clinical decision support
system described in Chapter~\ref{chap:methodology}. The system was run on the
completed subset of the Synthea-derived evaluation cohort and judged with the
LLM-as-Judge method from Section~\ref{sec:evaluation}. The chapter answers
three main questions. First, how often does the system find the target disease
in its differential diagnosis? Second, what changes when multi-level memory is
enabled? Third, how do the results compare with the literature reviewed in
Chapter~\ref{chap:background}? The chapter also reports per-agent reliability,
latency, example patient runs, dashboard review, and limitations.

% ─────────────────────────────────────────────────────────────────────────────
\section{Experimental Setup}
\label{sec:results_setup}
% ─────────────────────────────────────────────────────────────────────────────

\paragraph{Cohort.} The evaluation cohort consists of 160 Gold-layer
patients selected by the deterministic eligibility rules and the LLM-based
detectability verifier of Section~\ref{sec:llm_verifier}, and run end-to-end
through the seven-agent pipeline. These patients cover
the target disease groups used in the thesis: ischemic heart disease (IHD),
chronic congestive heart failure (CHF), essential hypertension, type-2
diabetes mellitus (T2DM), end-stage renal disease together with chronic
kidney disease stages 2--3 (reported together as ESRD~/~CKD), and
metabolic syndrome~X. The CKD substages are separate SNOMED labels in
the data, but they share one NICE management pathway, so they are
grouped for reporting. The aggregate file shows the principal 160 cohort
contains ESRD, CKD stage 3, and CKD stage 2 only; no CKD stage 1
patients are present. The agents do not see
the ground-truth condition during inference; only the evaluator receives it.

\paragraph{Models and infrastructure.}
The main reasoning model is GPT-OSS-120B served through Groq. The evaluator is
Qwen3-32B, also served through Groq. Both are accessed through the provider
adapter described in Section~\ref{sec:env}. Qdrant is used for NICE guideline
retrieval and case-based memory. The data pipeline, prompt rendering, agent
orchestration, and evaluation scripts run on an Apple Silicon workstation. The
Streamlit dashboard and React doctor console are served locally.

\paragraph{Pipeline configuration.}
All runs use the default values in \texttt{src/config.py}: maximum diagnostic
rounds of 3, confidence threshold of 75, an outer per-agent execution budget
of 300 seconds, a 120-second slow-call warning threshold in the provider
adapter (logged rather than enforced as per-call cancellation), and capped
exponential retry. Prompt templates are loaded from
\texttt{prompts/\{agent\_id\}.yaml}. No prompt or threshold was tuned after
seeing the reported results. Run files are saved under
\texttt{data/gold/mas\_results\_*/}, with one folder per patient UUID. Each
folder contains one JSON file per agent and an \texttt{execution\_trace.json}
timeline. The metrics below are computed from those files.

% ─────────────────────────────────────────────────────────────────────────────
\section{Evaluation Metrics}
\label{sec:results_metrics}
% ─────────────────────────────────────────────────────────────────────────────

The evaluator produces five main values for each patient. \textbf{Match type}
is DIRECT, INDIRECT, or MISS. DIRECT means the target disease appears in the
differential as a clear match. INDIRECT means the system found a clinically
related disease. MISS means it did not find the target disease or an acceptable
proxy. \textbf{Rank-when-found} records where the matched disease appears when
the judge returns DIRECT or INDIRECT. \emph{Rank-1-when-found} is the share of
found cases where the match is ranked first. \textbf{Per-agent status} records
success, partial, error, or skipped in \texttt{execution\_trace.json}.
\emph{Partial} means the output was recovered after a validation issue.
\emph{Error} means the agent failed and downstream agents carried a
\texttt{null} value. \emph{Skipped} means the agent was intentionally bypassed,
as with Treatment Planning when the match is not DIRECT. \textbf{Per-agent
latency} is the wall-clock time for each agent call. For memory, the chapter
reports an aggregate comparison across all available unique runs in each memory
setting and discusses cohort leakage as a limitation of interpreting memory
effects.

\paragraph{How rates are reported.}
The headline cohort table and the per-disease-family table report
Wilson 95\% confidence intervals on the headline diagnostic rates so
that the reader can see how much each percentage is constrained by
the sample size. The plain-language meaning of the CI is given right
under Table~\ref{tab:e2e_headline}. Other tables (memory comparisons,
model-pilot, agent reliability) report rates as descriptive summaries
without CIs --- those experiments are either underpowered for an
interval-based interpretation or are not the chapter's primary
claim. Where two proportions are compared with a test, the test is
named at the point of use: an exact McNemar for the paired
160-patient memory A/B (Section~\ref{sec:results_memory_ab}) and Fisher's exact
for the detectability-gate sensitivity check
(Section~\ref{sec:results_detectability_sensitivity}).

% ─────────────────────────────────────────────────────────────────────────────
\section{End-to-End Diagnostic Outcomes}
\label{sec:results_e2e}
% ─────────────────────────────────────────────────────────────────────────────

Table~\ref{tab:e2e_headline} shows the main diagnostic results on the
160 completed runs. The system returns a DIRECT match in 73.8\% of cases
(118/160, Wilson 95\% CI [66.4, 80.0]), an INDIRECT match in 13.8\%
[9.3, 19.9], and a MISS in 12.5\% [8.2, 18.5]. The combined found rate
(DIRECT + INDIRECT) is 87.5\% [81.5, 91.8]. When the target disease is
found, it is ranked first in 60.0\% [51.7, 67.7] of found cases.

\begin{table}[!htbp]
\centering
\caption{Headline diagnostic outcomes on the 160-patient completed cohort.
Wilson 95\% confidence intervals are shown in brackets.}
\label{tab:e2e_headline}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Match type}        & \textbf{Rate} & \textbf{95\% CI} \\
\midrule
DIRECT                     & 73.8\% & [66.4, 80.0] \\
INDIRECT                   & 13.8\% & [9.3, 19.9]  \\
MISS                       & 12.5\% & [8.2, 18.5]  \\
\midrule
Found (DIRECT + INDIRECT)  & 87.5\% & [81.5, 91.8] \\
Rank-1 within found        & 60.0\% & [51.7, 67.7] \\
\midrule
Median runtime / patient   & \multicolumn{2}{l}{129\,s (mean 192\,s)} \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{How to read the 95\% CI column.}
The 95\% confidence interval is the range the underlying rate is
likely to lie in, given how many patients were measured. The DIRECT
rate on the cohort is 73.8\%; the interval [66.4, 80.0] says that
even if the experiment were re-run on a different 160-patient sample
drawn from the same population, the rate would, with 95\% confidence,
fall somewhere between roughly 66\% and 80\%. The smaller the sample,
the wider the interval --- this is why some per-disease rows in the
next table carry CIs over 60 percentage points wide and should not be
read as reliable per-disease accuracy estimates.

Table~\ref{tab:e2e_family} breaks down the same cohort by disease family. Performance is strongest
on metabolic syndrome~X and ESRD~/~CKD. ESRD~/~CKD is also the largest
group and reaches a 98\% found rate. Smaller groups such as type-2
diabetes (n=8) and chronic congestive heart failure (n=5) carry wide
intervals: the CHF DIRECT point estimate of 60\% sits in a 95\% CI of
[23.1, 88.2], so it cannot be read as a reliable per-disease accuracy
estimate. The cohort is also imbalanced --- ESRD~/~CKD accounts for
42.5\% of the completed runs, while CHF and T2DM together account for
8.1\% --- which is a direct consequence of the Synthea module weights and
of the detectability-gate output discussed in
Section~\ref{sec:llm_verifier}. Essential hypertension is the main
outlier among the well-powered groups: the DIRECT rate is reasonable,
but the MISS rate is higher than for the other families. This appears
to come from Synthea cases where blood pressure evidence alone was not
enough for the agent to place hypertension in the top-five differential.

\begin{table}[!htbp]
\centering
\caption{Diagnostic outcomes by target disease family. CKD stages~2--3
plus ESRD are reported jointly because they share a single NICE
management pathway. The CI column is a Wilson 95\% interval on the
DIRECT rate. Rows marked $\dagger$ have $n \le 8$ and are not powered
for per-disease accuracy claims.}
\label{tab:e2e_family}
\small
\begin{tabular}{@{}lrrrrr@{}}
\toprule
\textbf{Disease family}            & \textbf{N} & \textbf{DIRECT} & \textbf{95\% CI} & \textbf{INDIRECT} & \textbf{MISS} \\
\midrule
ESRD / CKD                         & 68 & 73.5\%          & [62.0, 82.5]     & 23.5\%            & 2.9\%  \\
Metabolic syndrome X               & 32 & 90.6\%          & [75.8, 96.8]     & 0\%               & 9.4\%  \\
Essential hypertension             & 25 & 64.0\%          & [44.5, 79.8]     & 8.0\%             & 28.0\% \\
Ischemic heart disease             & 22 & 72.7\%          & [51.8, 86.8]     & 13.6\%            & 13.6\% \\
Type 2 diabetes mellitus$^\dagger$ &  8 & 50.0\%          & [21.5, 78.5]     & 12.5\%            & 37.5\% \\
Chronic congestive heart failure$^\dagger$ &  5 & 60.0\%   & [23.1, 88.2]     & 0\%               & 40.0\% \\
\bottomrule
\end{tabular}
\end{table}

% ─────────────────────────────────────────────────────────────────────────────
\subsection{Detectability-Gate Sensitivity}
\label{sec:results_detectability_sensitivity}
% ─────────────────────────────────────────────────────────────────────────────

The 73.8\% DIRECT headline is evaluated on a cohort that has already passed
the LLM-based detectability verifier described in
Section~\ref{sec:llm_verifier}, which admits only patients with a
confidence score of at least 80. This is a deliberate data-quality choice,
not a hidden filter, but it does mean the reported rate is conditional on
the gate. To quantify how much of the headline depends on this conditioning,
a sensitivity sample was drawn from the rejected band of the verifier
output (confidence 60--79), stratified evenly between the two ten-point
sub-bands and seeded so the selection is reproducible
(\texttt{scripts/sample\_rejected.py}, seed 42). The selected patients have
valid Gold-layer evidence and a Synthea ground-truth label; only the
detectability verifier judged them as borderline rather than clearly
diagnosable.

These thirty patients were then run end-to-end through the same seven-agent
pipeline, with results persisted to
\texttt{data/gold/mas\_results\_detectability\_sensitivity/}, and judged by
the same Qwen3-32B evaluator used for the main cohort. The intent is to
estimate the DIRECT rate the system would have achieved on cases the gate
currently excludes, and to test the corresponding 2$\times$2 contingency
against the accepted-cohort baseline with Fisher's exact test.

\paragraph{Sensitivity result.}
On the thirty rejected-band patients, the system recorded a DIRECT match
in 13.3\% of cases (4/30), an INDIRECT match
in 56.7\% [39.2, 72.6], and a MISS in 30.0\% [16.7, 47.9]. Compared with
the 73.8\% [66.4, 80.0] DIRECT rate on the accepted cohort, this is a
drop of 60.5 percentage points. A $\chi^2$ test on the 2$\times$2
contingency table of \{accepted vs.\ rejected\} $\times$ \{DIRECT vs.\
not DIRECT\} returns $\chi^2 = 40.13$, $p \approx 2.4 \times 10^{-10}$;
Fisher's exact test on the same table returns odds ratio
$0.055$, $p \approx 5.2 \times 10^{-10}$. The difference is therefore
not attributable to sampling noise at $n = 30$.

\paragraph{Reading the result.}
The gate is doing real selection work: the 73.8\% headline is
conditional on detectability rather than a population-level capability,
and the gap between the two conditions is large and statistically
unambiguous. The interpretation throughout this chapter is therefore
that CMADS produces a 73.8\% DIRECT rate \emph{on patients the
detectability verifier admits}, and a 13.3\% DIRECT rate on the
rejected band of the verifier. The INDIRECT rate on the rejected band
is comparatively high (56.7\%), which suggests the pipeline still
surfaces clinically related findings on borderline cases even when it
fails to identify the exact target disease. The 60-point DIRECT gap
across the gate also acts as an indirect post-hoc check on the
verifier itself: the verifier's confidence score is calibrated against
the same outcome the agent pipeline is later evaluated on, which is
the behaviour the gate was designed to produce. The raw counts and the
test outputs are persisted to
\texttt{data/\allowbreak gold/\allowbreak mas\_\allowbreak results\_\allowbreak detectability\_\allowbreak sensitivity/\allowbreak sensitivity\_\allowbreak summary.json}
for reproducibility.

% ─────────────────────────────────────────────────────────────────────────────
\section{Per-Agent Reliability and Latency}
\label{sec:results_agents}
% ─────────────────────────────────────────────────────────────────────────────

Table~\ref{tab:agent_reliability} reports per-agent status, and
Table~\ref{tab:agent_latency} reports per-agent wall-clock latency.
Diagnostic Reasoning, Clinical Reviewer, Diagnostic Refiner, and LLM
Evaluator complete successfully on almost every run. The EHR Analyst
and Lab Interpreter usually produce valid outputs, and when they do
not, the pipeline records a partial status and continues. Treatment
Planning is skipped when the evaluator returns INDIRECT or MISS,
which matches the DIRECT-gated design in Section~\ref{sec:agent_tx}.

\begin{table}[!htbp]
\centering
\caption{Per-agent reliability on the 160-patient cohort. \emph{Output
file present} = the agent produced its persisted JSON output;
\emph{Trace status} = the status string written into the run
trace. The two views diverge for the last two rows; the surrounding
prose explains why.}
\label{tab:agent_reliability}
\footnotesize
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{@{}lrrrrrr@{}}
\toprule
\textbf{Agent} & \textbf{Output file present} & \textbf{Success (trace)} & \textbf{Partial} & \textbf{Error} & \textbf{Skipped} & \textbf{Trace missing} \\
\midrule
EHR Analyst              & 100\% & 91\%  &  9\%  & 0\%   &  0\%   & 0\%   \\
Lab Interpreter          & 100\% & 94\%  &  6\%  & $<$1\%& 0\%    & 0\%   \\
Diagnostic Reasoning     & 100\% & 100\% &  0\%  & 0\%   &  0\%   & 0\%   \\
Clinical Reviewer        & 100\% & 99\%  &  1\%  & 0\%   &  0\%   & 0\%   \\
Diagnostic Refiner       & 100\% & 100\% &  0\%  & 0\%   &  0\%   & 0\%   \\
LLM Evaluator            & 100\% & 69\%  &  0\%  & 0\%   &  0\%   & 31\%  \\
Treatment Planning       & 96\%  & 46\%  &  0\%  & 3\%   & 20\%   & 31\%  \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Per-agent wall-clock latency on the 160-patient cohort,
computed from cases with a recorded trace entry.}
\label{tab:agent_latency}
\small
\begin{tabular}{@{}lrr@{}}
\toprule
\textbf{Agent} & \textbf{Mean} & \textbf{p95} \\
\midrule
EHR Analyst              & 28.1\,s &  31.9\,s \\
Lab Interpreter          & 31.9\,s &  52.2\,s \\
Diagnostic Reasoning     & 99.6\,s & 133.1\,s \\
Clinical Reviewer        & 38.3\,s &  56.4\,s \\
Diagnostic Refiner       & 10.9\,s &  16.5\,s \\
LLM Evaluator            &  1.2\,s &   1.9\,s \\
Treatment Planning       & 11.4\,s &  21.5\,s \\
\bottomrule
\end{tabular}
\end{table}

The 31\% \emph{trace missing} entries for the LLM Evaluator and
Treatment Planning rows are the same 49 of 160 cases, all from runs
that predate a telemetry change and therefore have output files but
no Stage~5 / Stage~6 status string. Read worst-case as in
Table~\ref{tab:agent_reliability}, those 49 cases are unknown; read
best-case (output file inspected directly), the Evaluator reaches
100\% success and Treatment Planning matches its DIRECT-gated design.
Seven runs additionally have no persisted \texttt{treatment\_planning.json}
file at all, and a small number of pre-telemetry MISS / INDIRECT
runs hold non-skip placeholders rather than the structured skip
described in Section~\ref{sec:agent_tx}; those are stale artifacts
from earlier code, not evidence of a treatment-gate failure.

Diagnostic Reasoning is the slowest stage, with a mean runtime of about
100 seconds and a 95th percentile near 133 seconds. This is expected because
the agent uses several LLM calls and may run more than one reasoning round.
Most other agents finish in under one minute on average. The LLM Evaluator is
fastest because it performs a short classification task. The reliability table
also shows the value of graceful degradation: when a Stage~1 output was only
partial, the later agents still ran and produced a final diagnosis. No patient
run failed completely because of an upstream agent error.

% ─────────────────────────────────────────────────────────────────────────────
\section{Adaptive Reasoning and Reviewer/Refiner Effects}
\label{sec:results_adaptive}
% ─────────────────────────────────────────────────────────────────────────────

Two design choices can be checked from the run files: the adaptive diagnostic
loop and the non-destructive Reviewer-Refiner pattern from
Section~\ref{sec:agent_refiner}.

The Reviewer-Refiner pattern had a visible effect. In about three out of five
runs, the Diagnostic Refiner changed the primary diagnosis originally produced
by the Diagnostic Reasoning agent (descriptive estimate from spot-checking
the persisted \texttt{diagnostic\_\allowbreak reasoning.json} versus
\texttt{final\_\allowbreak diagnosis.json} pairs in
\texttt{data/\allowbreak gold/\allowbreak mas\_results/}; a programmatic
recomputation across the full cohort is identified as future work and
falls under the out-of-scope items of the originating review). This happened because the Refiner does not
delete the original diagnostic output; it can change the final primary while
keeping the earlier output available for audit. The Reviewer also often flagged
anchoring on one strong lab value, such as creatinine pushing the agent toward
ESRD when the full patient history supported another primary condition. The
Refiner then re-weighted the differential using that critique.

The Diagnostic Reasoning agent also used its bounded self-critique loop. The
loop stops when confidence reaches 75 or after three rounds. In the memory runs
where \texttt{session\_memory.json} is available, most cases stopped after one
round, about a third stopped after two rounds, and the rest used all three
rounds. This saves time on easier cases and leaves more reasoning budget for
harder ones.

% ─────────────────────────────────────────────────────────────────────────────
\section{Multi-Level Memory A/B Study}
\label{sec:results_memory_ab}
% ─────────────────────────────────────────────────────────────────────────────

The memory subsystem is evaluated as a paired controlled experiment
on $n = 160$ patients. The same patient UUIDs are run with the
four-tier memory subsystem disabled and with it enabled; each patient
contributes one paired observation, and an exact McNemar test on the
off-diagonal gives the inferential result. This supersedes both the
earlier descriptive 205-vs-100 unpaired aggregate (which mixed
different patient subsets per arm) and the preliminary $n = 20$
paired checkpoint (exact McNemar $p = 1.0$): with identical patient
sets per arm and a tenfold-larger sample, the present test is
strictly more informative than either.

\paragraph{Judge prompt clarification for the A/B.}
During the design of this paired comparison it became visible that the
original judge prompt produced unstable classifications when the
system's diagnosis named the target disease together with an
etiological qualifier --- for instance ``Diabetic nephropathy leading
to end-stage renal disease'' for the ESRD target. The prompt's
original example list could be read either as DIRECT (the target is
named) or INDIRECT (an etiology is also named). For the paired A/B,
and \emph{only} for the paired A/B, the judge prompt was clarified to
make this case unambiguously DIRECT: when the system's diagnosis
explicitly names the target disease, it is DIRECT regardless of
additional etiological context. The clarified prompt is applied
consistently across both arms of the paired comparison. The headline
results in Section~\ref{sec:results_e2e} retain the original
judge run on the 160-patient cohort and are not affected by this
clarification. The two arms of the paired comparison use the
clarified prompt and are therefore internally consistent. Both versions
of every patient's judgement are preserved on disk
(\texttt{evaluation.json} for the version used in this section,
\texttt{evaluation\_strict.json} for the prior strict version).

In the \emph{single-level} setting, only the per-run
\texttt{PipelineState} is active --- no episodic event timeline, no
semantic disease store, no case-based Qdrant retrieval. In the
\emph{multi-level} setting, all four tiers are enabled. The
controlled variable is the memory configuration; patient identity,
prompts, evaluator, and model are held fixed across arms.

The multi-level arm draws from
\texttt{mas\_results\_improved\_b3/} (batch~3, 50 patients),
\texttt{mas\_results\_improved\_50/} (batch~4, 50 patients), and
\texttt{mas\_results\_improved\_extra60/} (60 additional patients
drawn from batches~1 and~5). The single-level arm draws from
\texttt{mas\_results\_paired65\_relaxed\_judge/} for the 65 UUIDs
that overlap with the original baseline cohort, and from
\texttt{mas\_results\_paired95\_single\_level/} for the 95 UUIDs
that were re-run with \texttt{MEMORY\_ENABLED=false}. The former
directory holds the clarified-judge evaluations for the overlap
UUIDs and is held separate from \texttt{mas\_results/} so the
headline numbers in Section~\ref{sec:results_e2e} remain
reproducible from \texttt{mas\_results/} under the original judge.
\texttt{scripts/paired\_160\_mcnemar.py} pairs the two arms by UUID
and computes the contingency and the exact McNemar test; the
persisted output is at
\texttt{data/gold/paired\_160\_mcnemar.json}.

Table~\ref{tab:memory_paired_160} shows the paired 2$\times$2
contingency under the clarified judge. The diagonal contains the
114 concordant pairs (76 both DIRECT, 38 neither DIRECT); the
off-diagonal contains the 46 discordant pairs --- 20 patients where
the single-level pipeline gave a DIRECT match but the multi-level
pipeline did not, and 26 patients where the reverse was true.
Multi-level memory wins six more discordant pairs than it loses.

\begin{table}[!htbp]
\centering
\caption{Paired contingency on $n = 160$ patients, single-level
versus multi-level memory under the clarified judge prompt. The
off-diagonal cells \textsc{only-single} and \textsc{only-multi} are
the discordant pairs that drive the exact McNemar test. Memory wins
26 discordant pairs against 20 it loses.}
\label{tab:memory_paired_160}
\footnotesize
\begin{tabular}{l c c}
\toprule
                                  & Multi-level $=$ DIRECT  & Multi-level $\neq$ DIRECT  \\
\midrule
Single-level $=$ DIRECT           & 76                      & 20                         \\
Single-level $\neq$ DIRECT        & 26                      & 38                         \\
\bottomrule
\end{tabular}
\end{table}

The DIRECT rate moved from \textbf{60.0\%} (96/160) with single-level
memory to \textbf{63.8\%} (102/160) with multi-level memory on the
same 160 patients --- a paired delta of \textbf{$+3.8$ percentage
points} in memory's favour. An exact (binomial) McNemar test on the
46 discordant pairs gives \textbf{$p = 0.46$}: the direction of
effect favours multi-level memory but does not reach statistical
significance at $\alpha = 0.05$. The signal is therefore directional
rather than confirmed --- 26 patients improved by adding memory
against 20 worsened, a 6-pair imbalance over 46 discordants.

Memory also produces a measurable shift on rank-1-within-found. On
the same paired cohort the multi-level configuration places the
correct diagnosis at rank 1 within the differential in 63.4\,\% of
found cases against 60.4\,\% for the single-level configuration ---
a 3.0\,pp gain. The complementary Found rate moves in the opposite
direction: multi-level recovers the correct disease as DIRECT or
INDIRECT in 90.6\,\% of patients against 93.1\,\% for the
single-level configuration. The pattern is internally consistent:
case-based retrieval concentrates probability mass on the disease
family suggested by the prior, which improves top-1 commitment but
slightly narrows the wider differential.

The earlier descriptive 205-vs-100 unpaired aggregate is retired.
Under the paired comparison with consistent judging, the apparent
twenty-percentage-point DIRECT gap of the unpaired comparison is a
cohort-selection artifact: the multi-level cohort (batches 3, 4, and
the supplementary extra-60 fills from batches 1 and 5) has a
different disease distribution than the patients in
\texttt{mas\_results/} that produced the headline number in
Section~\ref{sec:results_e2e}. When patient identity is held
constant, multi-level memory shows a small directional advantage on
DIRECT and an offsetting cost on Found, not the twenty-point gap of
the earlier descriptive read. The mechanism is consistent with the
qualitative pattern in Section~\ref{sec:results_qualitative}: when
the case-based prior retrieves a clinically similar past patient,
the Diagnostic Reasoning agent more often surfaces the right
disease family --- but sometimes follows the prior's family into an
INDIRECT match rather than the patient's exact Synthea label. The
Reviewer/Refiner step recovers some of this but not all of it. The
trade memory makes is recall over precision; whether the trade is
worth it depends on whether the downstream consumer reads the top-1
diagnosis or the full top-five differential.

An intermediate configuration --- Tier-4 case-based memory only,
with the other three tiers disabled --- was also run during
development on a fifty-patient subset. Its result was ambiguous: the
DIRECT rate was lower than the memory-disabled configuration but the
rank-1-within-found rate was higher, which suggests case-based
retrieval alone introduces the differential broadening seen in the
full multi-level configuration with weaker re-ranking signal. The
ambiguity at this size is part of why a dedicated tier-by-tier
ablation is identified as future work in
Chapter~\ref{chap:conclusion}.

The leakage risk is important. The case-based store does contain
patients used for evaluation, but retrieval explicitly filters out
the patient's own UUID and there are no near-duplicate inputs in the
Synthea-generated cohort beyond what disease-family similarity
naturally produces; the paired test on the same 160 patients in
both arms removes any cohort-composition contribution to the
comparison. The remaining interpretive caveat is the one above: the
mechanism we see in the data is differential broadening, not a
proven primary-diagnosis accuracy gain from memory.

% ─────────────────────────────────────────────────────────────────────────────
\section{Model Selection Pilot}
\label{sec:results_models}
% ─────────────────────────────────────────────────────────────────────────────

A small pilot compared the primary reasoning model on a fixed
twenty-patient subset. The prompts, patients, evaluator, and memory
setting were kept the same; only the diagnostic model differed.
The two models were GPT-OSS-120B through Groq and Med42-70B through
local Ollama. Table~\ref{tab:model_comparison} summarises the result.
It is reported here as a pilot, not as an inferential model comparison:
twenty patients is too small to separate the two DIRECT rates with
statistical confidence (the Wilson 95\% confidence intervals overlap
substantially, see below).

\begin{table}[!htbp]
\centering
\caption{Model comparison pilot on a fixed twenty-patient subset.
Identical prompts, evaluator, and memory configuration; only the
diagnostic model differs. At $n=20$ the DIRECT-rate comparison is
descriptive only; the wall-clock difference is the substantive
finding.}
\label{tab:model_comparison}
\footnotesize
\begin{tabular}{@{}lrrr@{}}
\toprule
\textbf{Diagnostic model}      & \textbf{DIRECT} & \textbf{Head-to-head wins} & \textbf{Wall-clock} \\
\midrule
GPT-OSS-120B (Groq, default)   & 50\%            & 8 of 20                    & $\sim$2~min/patient   \\
Med42-70B (Ollama, local)      & 25\%            & 2 of 20                    & $\sim$26~min/patient  \\
\bottomrule
\end{tabular}
\end{table}

In this pilot GPT-OSS-120B recorded a higher observed DIRECT point
estimate than Med42-70B (50\% vs.~25\% --- an underpowered pilot rather
than an inferentially supported accuracy claim) and won more
head-to-head comparisons (8 vs.~2, with the remaining ten equivalent).
The latency gap is the substantive finding: GPT-OSS-120B on Groq ran
about thirteen times faster than Med42-70B on local Ollama, well
beyond what twenty timed runs could be explained by noise. This does not imply that medical fine-tuning is unimportant.
It means that, in this pipeline, model quality and inference speed both
matter because latency adds up across seven agents and multiple
diagnostic rounds, and that a defensible accuracy comparison between
these two models would require a much larger common subset.

% ─────────────────────────────────────────────────────────────────────────────
\section{Comparison with the Literature}
\label{sec:results_literature}
% ─────────────────────────────────────────────────────────────────────────────

The papers reviewed in Chapter~\ref{chap:background} provide context for these
results, but direct accuracy comparison is difficult. The studies use different
datasets, tasks, and metrics. Some use multiple-choice benchmarks, some use
real specialty cases, and some use clinician ratings. These do not map directly
to DIRECT, INDIRECT, and MISS. For that reason, the comparison below focuses on
shared design points: coordination, memory, and evaluation method.
Table~\ref{tab:literature_comparison} summarises how the proposed
system sits relative to the closest direct comparators.

\begin{table}[!htbp]
\centering
\caption{Position of the proposed system relative to the most relevant
direct comparators in the multi-agent clinical AI literature.
Headline metrics are reproduced as reported in the original papers
and are not directly substitutable for the DIRECT~/~INDIRECT~/~MISS
metric used in this work.}
\label{tab:literature_comparison}
\footnotesize
\begin{adjustbox}{max width=\textwidth}
\begin{tabular}{@{}p{32mm}p{36mm}p{34mm}p{44mm}@{}}
\toprule
\textbf{System} & \textbf{Architecture} & \textbf{Evaluation cohort} & \textbf{Headline metric} \\
\midrule
MDAgents \newline \cite{Kim2024MDAgents}
  & Complexity-routed solo / group consultation
  & 10 medical reasoning benchmarks (MCQ + open-ended)
  & Best result on 7 of 10 benchmarks \\
ZODIAC \newline \cite{Zhou2024ZODIAC}
  & 5-agent cardiology with human review
  & Real cardiology patient data, 8 metrics
  & Cardiologist-level on 7 of 8 metrics \\
ClinicalLab \newline \cite{Yan2024ClinicalLab}
  & Coordinator + Chief Physician across 11 departments
  & 1{,}500 real cases from 11 hospital departments
  & Within 5\% of senior-physician diagnostic accuracy \\
MAC Framework \newline \cite{MAC2025npj}
  & MDT with supervisor; proposal-critique-refine
  & 302 rare-disease cases
  & Outperforms Self-Consistency at fewer LLM calls \\
RareAgents \newline \cite{Chen2024RareAgents}
  & MDT + persistent memory + tool use
  & RareBench + MIMIC-IV-Ext-Rare
  & Open-source backbone beats GPT-4o on rare-disease diagnosis \\
\midrule
This work
  & 7-agent LangGraph pipeline with non-destructive Reviewer-Refiner and 4-tier shared memory
  & Synthea patients filtered through deterministic eligibility and an LLM detectability verifier across 8 disease families
  & 73.8\% DIRECT, 87.5\% combined found rate, 60.0\% Rank-1 within found, plus an aggregate all-runs memory comparison \\
\bottomrule
\end{tabular}
\end{adjustbox}
\end{table}

Compared with MDAgents and MAC, this system uses the same broad idea of
specialised agents working on one clinical task. The difference is that CMADS
uses a fixed LangGraph workflow with parallel Stage~1 execution, a diagnostic
critique loop, and a non-destructive Reviewer-Refiner step. It also adds the
four-tier memory system and reports an all-runs memory comparison with the
leakage risk made explicit.

Compared with ZODIAC and ClinicalLab, a direct accuracy comparison would be
unfair because the cohorts are different. ZODIAC uses real cardiology data.
ClinicalLab uses real cases across eleven departments. This thesis uses
synthetic Synthea patients across several disease groups. The strength of this
setup is control: the same patients, prompts, and evaluator can be reused to
study design choices such as memory and model choice.

Compared with RareAgents, this work supports a similar lesson: workflow and
supporting tools can matter as much as the base model. RareAgents showed that
an open-source model with memory and tools can beat GPT-4o on rare-disease
diagnosis. In this thesis, GPT-OSS-120B inside the seven-agent workflow recorded a
higher observed DIRECT point estimate and a much lower per-patient
latency than the locally served Med42-70B model on the controlled
twenty-patient subset; the accuracy difference is descriptive at that
sample size, while the latency gap is the substantive finding. The
additional point here is the transparent memory comparison, including the
negative DIRECT-rate result and the cohort-leakage caveat, which is not usually
reported in memory-augmented clinical agent papers.

Bedi et al.'s Optimisation Paradox is also relevant
\cite{Bedi2025OptParadox}. Their work shows that combining individually strong
agents does not guarantee a strong clinical system. The results here support
the same practical message: the workflow, handoffs, review step, and memory
control are part of the system's performance, not secondary details.

% ─────────────────────────────────────────────────────────────────────────────
\section{Qualitative Case Walkthroughs}
\label{sec:results_qualitative}
% ─────────────────────────────────────────────────────────────────────────────

This section gives three short examples from the saved run files. The aim is
to show what the tables mean at patient level. The examples cover one DIRECT
case, one INDIRECT case, and one MISS.

\paragraph{Case~1 -- DIRECT match after review.}
\begingroup\sloppy
Patient
\texttt{7449ec96-\allowbreak b6ec-\allowbreak 0bb1-\allowbreak 63f0-\allowbreak 4f5943d35b73}
(run files under
\texttt{data/\allowbreak gold/\allowbreak mas\_\allowbreak results\_\allowbreak improved\_\allowbreak b3/})
had the target disease
\textit{Essential hypertension}. The first Diagnostic Reasoning output placed a
possible medication-induced electrolyte disturbance first, with essential
hypertension lower in the differential. The Clinical Reviewer judged this as
mis-ranked: the blood pressure pattern, obesity, and pre-diabetes gave stronger
support for primary hypertension than for an unconfirmed diuretic effect. The
Refiner changed the final primary diagnosis to \textit{Essential (primary)
hypertension}. The evaluator marked this as DIRECT at rank~1. The Treatment
Planner then generated a NICE NG136-based plan for hypertension, including
ramipril initiation, renal-function and potassium monitoring, blood-pressure
follow-up, and pregnancy/ethnicity checks before prescribing. This case shows
the value of keeping the reviewer and refiner separate: the system corrected
its first ranking before the final answer was saved.
\par\endgroup

\paragraph{Case~2 -- INDIRECT match with a related cardiac diagnosis.}
\begingroup\sloppy
Patient
\texttt{68089d6c-\allowbreak af03-\allowbreak 04f9-\allowbreak c862-\allowbreak 59358ecb3145}
(run files under
\texttt{data/\allowbreak gold/\allowbreak mas\_\allowbreak results\_\allowbreak improved\_\allowbreak b3/})
had the target disease
\textit{Ischemic heart disease}. The memory step found one previous DIRECT case
that passed the quality filter. The final primary diagnosis was still
\textit{Obstructive jaundice}, because the strongest evidence was a marked
bilirubin elevation and missing abdominal imaging. However, the differential
also included \textit{Heart-failure-related hepatic congestion} at rank~2. The
evaluator marked the case as INDIRECT because cardiac hepatopathy can be a
consequence of heart failure and ischemic heart disease. This example is useful
because it shows that the system can keep a related cardiovascular explanation
in the differential, but the top diagnosis may still follow the most obvious
laboratory abnormality.
\par\endgroup

\paragraph{Case~3 -- MISS where the target was not recovered.}
\begingroup\sloppy
Patient
\texttt{4fe0fcd1-\allowbreak c62b-\allowbreak c1dd-\allowbreak f27c-\allowbreak 2e43e90946c0}
(run files under
\texttt{data/\allowbreak gold/\allowbreak mas\_\allowbreak results\_\allowbreak improved\_\allowbreak b3/})
had the target disease
\textit{Ischemic heart disease}. The Diagnostic Reasoning output first focused
on resistant hypertension. The reviewer then pushed the case toward
\textit{Obstructive sleep apnea} as a likely secondary driver of hypertension,
and the Refiner kept obstructive sleep apnea as the final primary diagnosis.
The evaluator marked the case as MISS because none of the top five diagnoses
were a direct or indirect match for ischemic heart disease. This failure shows
a common weakness in the pipeline: known cardiovascular history can be treated
as background context instead of being carried forward as an active diagnostic
candidate.
\par\endgroup

% ─────────────────────────────────────────────────────────────────────────────
\section{Doctor-Facing Dashboard and Clinical Expert Review}
\label{sec:results_dashboard_review}
% ─────────────────────────────────────────────────────────────────────────────

The dashboard is important because it turns the saved JSON outputs into a form
a doctor can inspect. It shows the patient case, agent reasoning, retrieved
guidelines, memory use, and final diagnosis without forcing the reviewer to
read raw files.

\subsection{Dashboard walkthrough}
\label{subsec:results_dashboard}

The system has two review interfaces. The Streamlit dashboard at
\texttt{portal/dashboard.py} is used during development to inspect cohort
metrics and per-patient outputs. The React + FastAPI doctor console at
\texttt{doctor\_console/} is the clinician-facing version. It does not show the
hidden ground truth. Instead, it shows the agent chain, retrieved evidence,
final diagnosis, and similar prior cases.

The doctor console is organised around three views. The first view is the
patient list, where cases can be filtered by match status and basic patient
features. The second view is the assessment page for one patient. It shows the
ranked differential, the final diagnosis, treatment-plan summary when
available, and the saved outputs from each agent. The third view is the agent
flow. It follows the seven stages of execution and lets the reviewer inspect
the output of a selected agent without opening JSON files. This makes the
pipeline easier to explain in a viva or clinician review session, because the
reviewer can move from the patient record to the agent reasoning and then to
the final result in one screen.

The vector-search panel is especially important. It shows the top-$K$ similar
prior patients from case-based memory, ranked by BioLORD-2023 similarity. The
doctor can see their outcomes and evidence patterns. This makes memory use
inspectable. If memory affects the differential, the reviewer should be able to
see which prior cases influenced it.

\subsection{Planned Clinical Review Protocol}
\label{subsec:results_expert}

The clinical expert review has not been completed and is therefore not a
result of this thesis. This subsection describes the \emph{planned}
review protocol so that the doctor console can be assessed before
submission or future deployment; it is reported as future work rather
than as evidence of clinician-validated accuracy, and no result claims
are made from it.

\paragraph{Method.}
The reviewer will be shown ten patient cases from the evaluated cohort:
three DIRECT cases, three INDIRECT cases, two MISS cases, and two DIRECT cases
with treatment plans. For each case, the reviewer will see the doctor console
assessment view, including the ranked differential, reasoning summary,
retrieved NICE guideline chunks, and similar-past-cases panel. The reviewer
will not see the Synthea ground truth or the evaluator verdict. Each case will
be scored on four dimensions from 1 to 5, where 1 means unsafe or misleading
and 5 means clinically sound.

\paragraph{Scoring dimensions.}
\begin{itemize}
\item \textbf{Diagnostic plausibility} --- is the primary diagnosis a
  reasonable candidate given the presented evidence?
\item \textbf{Differential completeness} --- does the ranked
  differential cover the conditions a clinician would consider?
\item \textbf{Reasoning transparency} --- is the agents' reasoning
  chain inspectable and clinically intelligible?
\item \textbf{Treatment-plan safety} --- for DIRECT cases that produced
  a treatment plan, does the plan avoid contraindications and align
  with the cited NICE guideline?
\end{itemize}

\paragraph{Planned reporting.}
The review should be reported as a small table with the mean score for each
dimension and the number of cases scoring 4 or 5. Free-text comments should be
grouped into practical themes, such as unsafe treatment advice, missing
differential diagnoses, unclear reasoning, or useful memory explanations. Any
clinician comments must be kept separate from the LLM-as-judge results, because
they measure different things.

% ─────────────────────────────────────────────────────────────────────────────
\section{Discussion and Limitations}
\label{sec:results_discussion}
% ─────────────────────────────────────────────────────────────────────────────

The results show that the system can usually place the target disease, or a
closely related disease, in the differential diagnosis. The combined found rate
is 87.5\%, and the correct or related condition is ranked first in 60.0\% of
found cases (Rank-1 within found). The pipeline also completed without any full patient-run aborts. This
supports the main design choice of the thesis: a coordinated workflow of
specialised agents is more inspectable and reliable than asking one model to do
the whole task in one call.

The memory result needs careful reading. Multi-level memory did not improve the
DIRECT rate in the aggregate comparison. Instead, it increased INDIRECT cases,
raised the found rate, and reduced MISS cases. This means memory helped the
system surface related diagnoses, but it also made the primary diagnosis less
precise. This may still be useful in a dashboard where a doctor reads the full
differential. It would be less useful in a system where the first diagnosis
automatically drives the next action.

The model comparison also matters for practical use. On the tested
subset, GPT-OSS-120B through Groq recorded a higher observed DIRECT
point estimate than Med42-70B through local Ollama; the Wilson 95\%
confidence intervals on that comparison overlap at $n = 20$, so the
accuracy difference is read as exploratory rather than as established
evidence that GPT-OSS-120B is more accurate. The latency gap is the
substantive finding: GPT-OSS-120B on Groq was about thirteen times
faster than Med42-70B on local Ollama, and that gap is several orders
of magnitude larger than the noise in twenty timed runs. In a
multi-agent pipeline, every extra model call adds time, so inference
speed is not a minor implementation detail. It affects whether the
system can be used interactively.

\paragraph{Token and cost accounting.}
The LLM adapter does not currently persist token counts, so direct
per-patient token consumption is not reported. A first-order cost
estimate can still be derived from the persisted agent outputs and
the input case files: averaging across the 160-patient cohort, each
patient run uses an estimated 80--90~k input tokens and 18--22~k
output tokens spread across the seven agents, with the Diagnostic
Reasoning agent dominating because of its multi-round adaptive loop.
At Groq's published rates (GPT-OSS-120B at \$0.15 / M input, \$0.75
/ M output; Qwen3-32B at \$0.29 / M input, \$0.59 / M output) this
gives an estimated cost of \textbf{approximately \$0.03 per patient
run} (reasonable range \$0.01--\$0.04 depending on diagnostic-round
count and treatment-planning eligibility). At this rate, the
160-patient cohort cost approximately \$5; a 1{,}000-patient
evaluation would cost approximately \$30. The estimate excludes
embedding and Qdrant infrastructure costs (both negligible at this
scale) and is conservative on the input side because the Diagnostic
Reasoning loop's intermediate-call inputs are not separately
persisted. Direct token instrumentation in the LLM adapter is
identified as future work; once added, the estimate above can be
replaced with measured numbers per patient.

The results have clear limits. All patients are synthetic and come from Synthea.
The detectability verifier selects cases where the target disease is visible in
the available data, so the cohort is cleaner than real EHR data. Real patient
records would include more missing values, noisier histories, and more complex
comorbidities. Some disease families, especially type-2 diabetes and chronic
congestive heart failure, have small counts, so their per-family percentages
should not be overinterpreted. The memory comparison is also limited in size and
should be repeated on a larger and more balanced cohort. Finally, this is a
research prototype, not a deployable medical device. The dashboard is intended
to support review by a clinician, and the clinician remains the decision maker.
