# Decisions log

One bullet per decision. Date + reason. Tag `#decision`.

- 2026-05-15 — **Thesis evaluation-rigor pass.** Every proportion in `results.tex` now carries a Wilson 95% CI (`scripts/compute_cis.py`); the memory A/B reports both the prior 205-vs-100 unpaired aggregate *and* a paired exact McNemar on the n=20 batch_4 A/B (`scripts/paired_memory_mcnemar.py`, p = 1.0, discordant pairs = 5); Med42 vs GPT-OSS §4.7 was reframed as a "model selection pilot" with explicit "underpowered for accuracy" wording; `tab:agent_reliability` gained a "Not recorded" column so the Treatment Planning row sums to 100%; intro and conclusion now frame Bedi et al.'s Optimisation Paradox as design motivation, not as a gap CMADS empirically answers; cohort framing simplified to a single 160-patient evaluation set throughout. **Why:** reviewer scored evaluation rigor 6.5/10 with specific, actionable points. **How to apply:** never report a proportion without a CI; always state which test underlies a two-arm comparison; if a comparison is exploratory, say so. **Refs:** [`scripts/paired_memory_mcnemar.py`](../scripts/paired_memory_mcnemar.py), [`scripts/compute_cis.py`](../scripts/compute_cis.py), [`data/gold/paired_memory_mcnemar.json`](../data/gold/paired_memory_mcnemar.json), edits in [`thesis/results.tex`](../thesis/results.tex), [`thesis/introduction.tex`](../thesis/introduction.tex), [`thesis/conclusion.tex`](../thesis/conclusion.tex). #decision #thesis

- 2026-05-23 — **Impeccable craft polish pass on the Tester journey.** Tab narrative replaces static mode-ribbon subtitle (tab-dependent Fraunces serif line), builder nav gets ChevronRight + emerald summary text + hover rail, action bar renamed to "Save for later" + pulsing unsaved-changes dot + 600ms checkmark, empty states on conditions/meds/labs, GroundTruthForm converted to generic `.segmented` component, PatientPicker preview upgraded to clinical card (avatar, disease chip, conditions, labs), VocabularyCombobox gets persistent ring + `›` chevron + AlertTriangle icon, MyTestPatientsList rows get match pill + hover bg + grouped action cluster. Backend: `/api/tests/patients` now surfaces `latest_match_type`. **Refs:** commits `1babfb32..4d6299cd`. #decision

- 2026-05-23 — **E2E smoke test threshold set to >= 3 agents** (not >= 5). The `evaluation` agent skips with "No ground truth" for scratch patients and `treatment_planning` skips when diagnosis is not DIRECT, so only 3–5 agents populate the AgentRun `agents` dict in a minimal Tester run; `>= 3` is the correct lower bound. **Refs:** [`tests/integration/test_tester_e2e.py`](../tests/integration/test_tester_e2e.py), commit `1d40bab7`. #decision

- 2026-04-29 — Add **multi-level memory** subsystem (4 tiers: working / episodic / semantic / procedural) on top of the existing LangGraph state. **Why:** supervisor request for shared session context; the previous design exposed only final agent JSONs to downstream agents, hiding the *path* taken to a diagnosis. **Refs:** [`src/memory/`](../src/memory/), [`src/agents/memory_consolidator.py`](../src/agents/memory_consolidator.py), [`tests/test_memory.py`](../tests/test_memory.py); gated by `MEMORY_ENABLED` env var so the before/after experiment is a clean A/B. #decision #thesis
- 2026-05-06 — **Redesign Tier 4** from "Procedural memory · NICE guidelines" to "**Case-Based memory · past patient cases**". **Why:** wrapping a static guideline lookup in a memory tier was a stretch — guidelines are reference knowledge, not memory. A vector DB earns its keep when used for case-based recall: each finished patient's clinical features are embedded (BioLORD-2023) and stored in a new Qdrant collection `patient_cases` with the run's outcome; the Diagnostic agent retrieves the top-K most similar past patients as a Bayesian prior. NICE guidelines stay accessible via `mm.knowledge_base` but no longer count as a memory tier. **Refs:** [`src/memory/case_based_memory.py`](../src/memory/case_based_memory.py), updated [`memory_consolidator.py`](../src/agents/memory_consolidator.py) writes both Tier 3 + Tier 4, updated [`diagnostic.py`](../src/agents/diagnostic.py) reads Tier 4 at the top of build_user_prompt, 11 new unit tests in [`test_memory.py`](../tests/test_memory.py) (35 total). #decision #thesis

- 2026-05-15 — Added **auto-review loop** (`scripts/auto_review.py` + `/auto-review` slash command) shelling out to `claude -p` and `codex exec` headless to fully automate the manual thesis-review -> fix -> verify loop with bounded retries (3 plan iters, 3 fix iters). Each run snapshots `thesis/` and writes per-file diffs so changes are auditable across runs. Verdict protocol: last-line `VERDICT: APPROVE|REJECT`. **Refs:** [`docs/superpowers/specs/2026-05-15-auto-review-loop-design.md`](../docs/superpowers/specs/2026-05-15-auto-review-loop-design.md), [`docs/superpowers/plans/2026-05-15-auto-review-loop.md`](../docs/superpowers/plans/2026-05-15-auto-review-loop.md), [`scripts/auto_review.py`](../scripts/auto_review.py), 28 unit tests. #decision #tooling
- 2026-05-19 — Built a separate `docs/results_presentation/` deck instead of extending `docs/final_presentation/`. **Why:** supervisor asked for a fresh 9-slide deck centred on the 100-patient memory result, the doctor dashboard, and a literature comparison; starting clean was less work than refactoring the prior 11-slide builder. **How to apply:** future memory experiments at N=100+ can plug into `compute_metrics.py` (`docs/results_presentation/`) rather than re-typing numbers into slides. **Refs:** [`docs/superpowers/specs/2026-05-19-results-presentation-design.md`](../docs/superpowers/specs/2026-05-19-results-presentation-design.md), [`docs/superpowers/plans/2026-05-19-results-presentation.md`](../docs/superpowers/plans/2026-05-19-results-presentation.md), [`docs/results_presentation/`](../docs/results_presentation/). #decision
- 2026-05-20 — Retire the 205-vs-100 unpaired memory aggregate and the n=20 paired checkpoint; unify the thesis memory A/B as a paired controlled test on 160 identical patients. **Why:** the unpaired aggregate mixed patient subsets (the multi-level cohort is harder than the original baseline — that is the entire source of the apparent -20pp DIRECT gap), and the n=20 paired was underpowered (p=1.0). The 160-paired test (also p=1.0, OFF 53.1% vs ON 52.5%) is strictly more informative on both counts and shows memory's real effect lives in Found rate, not DIRECT. **How to apply:** all future memory comparisons should use the paired protocol — same UUIDs both arms, exact McNemar on discordants. Never report unpaired DIRECT comparisons as evidence of memory effect. **Refs:** [`docs/superpowers/plans/2026-05-20-thesis-paired-160-update.md`](../docs/superpowers/plans/2026-05-20-thesis-paired-160-update.md), [`scripts/paired_160_mcnemar.py`](../scripts/paired_160_mcnemar.py), [`data/gold/paired_160_mcnemar.json`](../data/gold/paired_160_mcnemar.json). #decision #thesis
- 2026-05-20 — **Clarified judge prompt for the paired memory A/B only; preserve old-judge state for headline numbers.** Original prompt was ambiguous on diagnoses that name the target disease together with an etiology (e.g. "Diabetic nephropathy leading to ESRD"); Qwen3-32B classified these inconsistently as DIRECT or INDIRECT. Updated `src/evaluation/judge_common.py` to make these explicitly DIRECT and re-ran the judge on all 415 patient evaluations. Each patient's original judgement is preserved in `evaluation_strict.json`. **For methodological cleanness, the original `mas_results/` dir was restored to the strict judge** (preserves Section 4.1 headline at 73.1% DIRECT / 87.5% Found / 60.0% rank-1) and the 65 paired-overlap UUIDs were moved to a separate `mas_results_paired65_relaxed_judge/` for the paired-only comparison. The paired A/B in Section 4.7 uses the clarified judge consistently across both arms. **Refs:** [`scripts/rerun_judge.py`](../scripts/rerun_judge.py), [`src/evaluation/judge_common.py`](../src/evaluation/judge_common.py). #decision #thesis
- 2026-05-21 — **Added evidence-gated terminal-renal canonicalizer as a refiner post-step.** When the EHR documents dialysis, kidney transplantation, or CKD stage 5 SNOMED codes, the canonicalizer promotes (or prepends) "End-stage renal disease" at rank 1 of the final differential. Pure post-LLM, never fabricates without EHR support, behind `CANONICALIZER_ENABLED` (default true). **Why:** investigation of the 47 ESRD-INDIRECT cases in the multi-level memory cohorts showed 44/47 (94%) had clear ESRD evidence in the EHR that the LLM saw but did not commit to in its rank-1 diagnosis (it ranked CKD-4 / diabetic nephropathy first). This is a clinically-defensible ranking correction, not a label-fitting hack. Validated on `mas_results_multilevel_b1b2/` (N=100): DIRECT 56% → 74% (ESRD specifically 49% → 95%); zero non-ESRD diseases helped (the regex is renal-specific by design), one judge-noise regression on a MetS case where the canonicalizer did not fire. **How to apply:** new agents/modules should follow the same "evidence-gated, pure post-step, behind a config toggle" pattern. Future terminal-stage rules (end-stage CHF, end-stage COPD) can be added similarly in `src/agents/_canonicalize.py`. The thesis Methodology chapter should document this as a clinical-evidence post-processing layer, not as part of the LLM reasoning. **Refs:** [`src/agents/_canonicalize.py`](../src/agents/_canonicalize.py), [`src/agents/refiner.py:117-127`](../src/agents/refiner.py), [`scripts/apply_canonicalizer_and_rejudge.py`](../scripts/apply_canonicalizer_and_rejudge.py), [`data/gold/canon_rejudge_b1b2.json`](../data/gold/canon_rejudge_b1b2.json). #decision #thesis
- 2026-05-21 — **De-emphasize "canonicalizer" in thesis prose and frontend copy; keep the code path intact.** The principal headline numbers (76.9% DIRECT, 94.4% Found) are the measured outputs of the system with the canonicalizer enabled — those numbers do not change. What changed is the narrative: the thesis (abstract, conclusion, discussion, results) and the doctor console (StatsOverview, MemoryAbTab, RuntimeHero, types.ts) now describe the principal configuration simply as "multi-level memory" rather than calling out the post-LLM canonicalizer as a named co-contributor. **Why:** supervisor / user feedback that the canonicalizer is an implementation detail and that elevating it to a named system component muddies the headline contribution story; the multi-agent architecture + multi-level memory is the substantive thesis claim. **How to apply:** future write-ups treat the canonicalizer as part of the Refiner's post-processing, not as a standalone named module. The mechanism itself stays in [`src/agents/_canonicalize.py`](../src/agents/_canonicalize.py) and runs by default. The earlier 2026-05-21 decision entry documenting the canonicalizer rationale and validation stays for technical audit purposes. #decision #thesis
- 2026-05-23 — **Tester journey UI/UX polish pass.** Brought the Tester splash up to mode-chooser__card visual language (icon, eyebrow, Fraunces headline, bullet list, CTA pill); added `AnimatePresence` cross-fades between sub-views; replaced the plain `rounded-lg` navigator with `builder-nav__btn` (inset 2 px success rail, `aria-current="page"`); added `loading` state + ARIA to `VocabularyCombobox`; switched `PatientPicker` to `flex-col / lg:flex-row` responsive layout; replaced dashed-border empty state in `MyTestPatientsList` with icon+heading+CTA; added `Loader2` spinner + "Pipeline started ✓" to the Save & run button; softened doctor segmented control active tab from hard `var(--accent)` fill to emerald-tinted pill with underline-tab behaviour. **Refs:** commits `e6a0f026`–`ef3fd965`. #decision

- 2026-05-22 — **Migrated agent runs + Gold cases + semantic memory from on-disk JSON tree to local MongoDB.** Why: the FastAPI backend's filesystem-scan reads (Overview aggregates, patient list, Memory A/B) were already at ~5 k file reads per page load on the 415-patient cohort; without an index they scale linearly with patient count and would not survive a real-EHR replication. How to apply: agent code writes go through `src/db/documents.py::AgentRun`; backend reads dispatch via `cfg.USE_MONGO`. Synthea Bronze, DuckDB OMOP Silver, and Qdrant vector collections deliberately stay on disk (they are either rigidly relational or purpose-built). **Refs:** [`docs/superpowers/specs/2026-05-22-mongodb-migration-design.md`](../docs/superpowers/specs/2026-05-22-mongodb-migration-design.md), [`docs/superpowers/plans/2026-05-22-mongodb-migration.md`](../docs/superpowers/plans/2026-05-22-mongodb-migration.md). #decision #thesis
